{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Default logging level for all categories
      "Microsoft.AspNetCore": "Warning", // Quieter logging for ASP.NET Core framework components
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning" // Quieter logging for EF Core commands by default
    }
  },
  "AllowedHosts": "*", // For production, consider restricting this to your specific domain(s)
  "ConnectionStrings": {
    // Your primary database connection string for SQL Server Express.
    "RestTestConnection": "Server=.\\SQLEXPRESS;Database=REST_TEST;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true",
    // Connection string for the database containing VIEW_EISOD_SD.
    "EisodConnection": "Server=NTSPPR;Database=dbPPRoiDIS;Integrated Security=True;TrustServerCertificate=True;Connect Timeout=30;"
  },
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://localhost:5210" // Or your preferred HTTP port
      },
      "Https": {
        "Url": "https://localhost:5211" // Or your preferred HTTPS port
        // For production, you'd configure your actual SSL certificate here or use a reverse proxy like Nginx/IIS.
        // "Certificate": {
        //   "Path": "<path_to_your_pfx_file>",
        //   "Password": "<your_certificate_password>"
        // }
      }
    },
    "Certificates": {
      "Default": {
        // In development, ASP.NET Core can use a development certificate.
        // For production, specify your certificate details if Kestrel is handling SSL directly.
      }
    }
  }
  // --- Authentication Settings ---
  // This section would be populated based on your chosen authentication mechanism.
  // For example, if using JWT:
  // "JwtSettings": {
  //   "Issuer": "your-api-issuer",
  //   "Audience": "your-api-audience",
  //   "Key": "YOUR_VERY_SECRET_AND_STRONG_KEY_REPLACE_THIS_AND_STORE_SECURELY", // Store this securely, e.g., User Secrets or Azure Key Vault
  //   "DurationInMinutes": 60
  // }
}